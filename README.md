# Dress Website

Project is split into separate frontend and backend folders.

## Frontend

```bash
cd client
npm install
npm run dev
```

## Backend

```bash
cd server
npm install
npm run dev
```

Backend uses `.env` inside the `server` folder.
